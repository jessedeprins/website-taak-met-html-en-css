.hero-image {
    background: url("../images/chezjesse.png") center/cover no-repeat;
    height: 300px;
}

body {
    margin: 0;
    font-family: Arial, sans-serif;
}

header {
    background: lightgray;
    padding: 20px;
}

nav a {
    margin-right: 15px;
    color: black;
    text-decoration: none;
    background: lightgray;
}

main {
    padding: 20px;
    background: lightgray;
}

section {
    margin-bottom: 20px;
    background: lightgray;
}

img {
    border-radius: 10px;
}

.username {
    font-weight: bold;
}

/* Locatie sectie layout */
.locatie-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    flex-wrap: wrap;
}

.locatie-tekst {
    flex: 1;
    min-width: 200px;
}

.locatie-afbeelding {
    flex: 1;
    min-width: 200px;
}

.locatie-afbeelding img {
    width: 100%;
    max-width: 400px;
}

.menu-galerij {
    text-align: center;
    margin: 40px auto;
}

.galerij-grid {
    display: flex;
    flex-direction: column;
    gap: 20px;
    align-items: center;
}

.rij {
    display: flex;
    gap: 20px;
    justify-content: center;
}

.rij img {
    width: 250px;
    height: 250px;
    object-fit: cover;
    border-radius: 10px;
}

/* Onderste rij gecentreerd onder foto 2 en 3 */
.rij-onder {
    justify-content: center;
}

.spacer {
    width: 200px;
    height: 1px;
}
